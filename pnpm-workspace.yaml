packages:
  - "apps/*"
  - "packages/*"

# Dependencies allowed to run install scripts (pnpm 12 blocks them by default
# and fails CI with ERR_PNPM_IGNORED_BUILDS unless allowed here).
allowBuilds:
  esbuild: true
