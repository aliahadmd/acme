packages:
  - "apps/*"
  - "packages/*"

# Dependencies allowed to run install scripts (pnpm blocks them by default).
# CI fails with ERR_PNPM_IGNORED_BUILDS without this.
onlyBuiltDependencies:
  - esbuild
